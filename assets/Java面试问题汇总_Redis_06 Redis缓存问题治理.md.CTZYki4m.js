import{_ as t,o as a,c as d,a2 as i}from"./chunks/framework.F7eQ-ohH.js";const r="/bzygq/assets/e28b05ff72064b5e93908093649b219c.D3uLlDfZ.png",l="/bzygq/assets/ed6e36a732d6489c9d3637bcacbff2d7.DwSJ1Oib.png",b=JSON.parse('{"title":"06 Redis缓存问题治理","description":"","frontmatter":{},"headers":[],"relativePath":"Java面试问题汇总/Redis/06 Redis缓存问题治理.md","filePath":"Java面试问题汇总/Redis/06 Redis缓存问题治理.md","lastUpdated":1788929995000}'),h={name:"Java面试问题汇总/Redis/06 Redis缓存问题治理.md"};function o(s,e,y,p,k,n){return a(),d("div",null,[...e[0]||(e[0]=[i('<h1 id="_06-redis缓存问题治理" tabindex="-1">06 Redis缓存问题治理 <a class="header-anchor" href="#_06-redis缓存问题治理" aria-label="Permalink to &quot;06 Redis缓存问题治理&quot;">​</a></h1><p>Redis 缓存治理不是把“击穿、穿透、雪崩”三个名词背下来，而是在真实故障里快速判断：流量为什么没有被缓存挡住，压力是落到了单个 key、非法 key、大片 key，还是落到了 Redis 实例本身。只有先把问题定位清楚，后面的互斥锁、布隆过滤器、TTL 随机、限流降级、BigKey 拆分、HotKey 分散读压力才不会用错地方。</p><p>从工程角度看，缓存层的稳定性取决于三件事：第一，正常请求能否命中缓存；第二，缓存失效时能否保护数据库；第三，Redis 自己出现慢、满、不可用时，业务链路有没有兜底。面试里问缓存问题，通常也是沿着这三件事追问：先问现象和概念，再问方案选择，最后问你在项目里如何发现、治理和复盘。</p><h2 id="一、先诊断-再治理" tabindex="-1">一、先诊断，再治理 <a class="header-anchor" href="#一、先诊断-再治理" aria-label="Permalink to &quot;一、先诊断，再治理&quot;">​</a></h2><p>缓存故障的第一步不是立刻加锁，也不是立刻加布隆过滤器，而是确认未命中的请求属于哪一类。一个实用的判断顺序是：先看是不是单个热点 key 失效，再看是不是大量不存在的数据被查询，再看是不是大面积 key 同时失效或 Redis 整体不可用，最后再补充检查 BigKey、HotKey、慢命令和网络流量。</p><p><img src="'+r+'" alt="images/redis-18-缓存问题诊断树.png"></p><p>images/redis-18-缓存问题诊断树.png</p><p>三类经典问题的区别可以先这样记：</p><table tabindex="0"><thead><tr><th>问题</th><th>典型现象</th><th>根因关键词</th><th>主要风险</th><th>治理重点</th></tr></thead><tbody><tr><td>缓存击穿</td><td>某个热点 key 过期后，数据库瞬时 QPS 飙升</td><td>单个热点 key、同一时刻失效</td><td>热点数据把数据库打穿</td><td>互斥锁、逻辑过期、热点预热、热点永不过期</td></tr><tr><td>缓存穿透</td><td>缓存和数据库都查不到，且无效请求很多</td><td>数据本来不存在、随机 ID、恶意请求</td><td>无效流量绕过缓存直达数据库</td><td>参数校验、布隆过滤器、空值缓存、风控限流</td></tr><tr><td>缓存雪崩</td><td>大量 key 同时失效，或 Redis 整体不可用</td><td>大面积过期、缓存层故障</td><td>请求集中打到底层存储，服务级联故障</td><td>TTL 随机、预热、多级缓存、限流降级、兜底页</td></tr></tbody></table><p>排查时可以按下面的顺序走：</p><ol><li>看数据库压力是否与缓存未命中同时上升。如果数据库慢查询、连接池等待、缓存 miss rate 同时升高，说明缓存层没有挡住流量。</li><li>看未命中 key 的分布。如果集中在一个或少数几个 key，多半是击穿或 HotKey；如果 key 高度离散且数据库查不到，多半是穿透；如果一批 key 同时过期，多半是雪崩。</li><li>看 Redis 实例指标。如果 Redis CPU、网络流量、内存、慢日志异常，问题可能不是普通缓存未命中，而是 BigKey、HotKey、慢命令、删除阻塞或实例不可用。</li><li>看业务时间点。大促开始、发布重启、定时任务刷新、批量导入、配置同步、热门内容上线，都可能触发集中预热或集中失效。</li><li>看治理动作是否产生副作用。比如互斥锁可能导致线程等待，空值缓存可能污染缓存，布隆过滤器可能误判存在，local cache 可能带来短暂不一致。</li></ol><p>这一步的价值在于避免“把所有缓存问题都当成击穿”。如果真实问题是穿透，加互斥锁只会让非法请求排队；如果真实问题是 Redis 整体不可用，给某个 key 加锁也保护不了数据库。</p><h2 id="二、缓存击穿-热点-key-失效后的并发回源" tabindex="-1">二、缓存击穿：热点 key 失效后的并发回源 <a class="header-anchor" href="#二、缓存击穿-热点-key-失效后的并发回源" aria-label="Permalink to &quot;二、缓存击穿：热点 key 失效后的并发回源&quot;">​</a></h2><p>缓存击穿指一个访问量很高的 key 在过期瞬间失效，大量请求同时没有命中缓存，于是一起访问数据库或下游服务。它的核心特征是“热点 key”和“失效窗口重叠”。商品详情、秒杀库存、直播间信息、首页配置、榜单数据都可能出现这种问题。</p><p>击穿的诊断重点是确认压力是否集中在少数 key 上。可以从应用日志、网关采样、缓存埋点、Redis 命令统计和数据库查询条件入手。如果未命中请求高度集中在同一个商品 ID 或活动 ID，并且时间点恰好接近缓存过期，就可以优先按击穿处理。</p><h3 id="_1-互斥锁-让一个线程回源-其他线程等待或快速失败" tabindex="-1">1. 互斥锁：让一个线程回源，其他线程等待或快速失败 <a class="header-anchor" href="#_1-互斥锁-让一个线程回源-其他线程等待或快速失败" aria-label="Permalink to &quot;1\\. 互斥锁：让一个线程回源，其他线程等待或快速失败&quot;">​</a></h3><p>互斥锁的思路是：缓存未命中后，不让所有请求都查数据库，而是让第一个请求抢锁，抢到锁的请求负责查库并回填缓存；其他请求短暂等待、重试缓存，或者直接返回降级结果。</p><p>典型流程是：</p><ol><li>查询缓存，命中则直接返回。</li><li>未命中后尝试获取分布式锁，例如 SET lock:key value NX EX 10。</li><li>获取锁成功后再次查询缓存，避免别的线程已经完成回填。</li><li>仍未命中才访问数据库，写入缓存并释放锁。</li><li>获取锁失败的请求短暂 sleep 后重试缓存，或走限流、排队、降级逻辑。</li></ol><p>互斥锁的优点是保护数据库效果直接，适合热点 key 数量有限、回源成本高、数据必须尽量新鲜的场景。代价是请求会在锁外等待，尾延迟可能上升；如果锁超时时间设置不合理，还可能出现锁提前过期、多个线程同时回源的问题。因此实现时要注意锁 value 唯一、释放锁用 Lua 校验 value、锁 TTL 覆盖正常回源耗时，并给等待请求设置最大等待时间。</p><p>面试追问常见在两个点：一是为什么获取锁后还要再查一次缓存，因为这是为了避免重复回源；二是锁超时怎么设置，回答时不要只说“设置长一点”，而要结合数据库查询耗时、网络抖动和重试策略，并说明超过阈值后应该降级而不是无限等待。</p><h3 id="_2-逻辑过期-允许短暂旧数据-换取稳定响应" tabindex="-1">2. 逻辑过期：允许短暂旧数据，换取稳定响应 <a class="header-anchor" href="#_2-逻辑过期-允许短暂旧数据-换取稳定响应" aria-label="Permalink to &quot;2\\. 逻辑过期：允许短暂旧数据，换取稳定响应&quot;">​</a></h3><p>逻辑过期不是让 Redis key 真正过期，而是在 value 中额外保存一个业务过期时间。请求读到数据后，如果发现逻辑时间已过期，可以先返回旧值，同时触发后台线程刷新缓存。这样前台请求不会在过期瞬间集体打到数据库。</p><p>它适合读多写少、可以接受短暂旧数据的热点场景，例如热门商品详情、榜单、活动页配置。它的代价也很清楚：数据新鲜度会降低，缓存里要存更复杂的结构，后台刷新任务要做幂等和并发控制。如果业务要求强一致，例如支付状态、库存扣减结果，就不能简单用逻辑过期返回旧值。</p><p>逻辑过期常和互斥刷新配合：只有一个线程负责异步刷新，其他线程继续读旧值。这样既避免击穿，也避免后台刷新任务本身把数据库打满。</p><h3 id="_3-热点预热和热点永不过期-把风险前移" tabindex="-1">3. 热点预热和热点永不过期：把风险前移 <a class="header-anchor" href="#_3-热点预热和热点永不过期-把风险前移" aria-label="Permalink to &quot;3\\. 热点预热和热点永不过期：把风险前移&quot;">​</a></h3><p>如果热点 key 在活动开始前就确定，例如秒杀商品、直播间配置、首页核心推荐，可以在流量到来之前预热到 Redis。对于极少数核心热点，还可以采用“物理不过期 + 后台定时刷新”的方式，避免自然 TTL 触发击穿。</p><p>预热的代价是需要维护热点识别和加载流程。预热太少挡不住流量，预热太多会浪费内存甚至压垮数据库；预热任务失败还可能让系统在活动开始时裸奔。因此预热不是简单 for 循环塞缓存，而要有任务分片、失败重试、限速、幂等、监控和人工确认。</p><h2 id="三、缓存穿透-请求的数据本来不存在" tabindex="-1">三、缓存穿透：请求的数据本来不存在 <a class="header-anchor" href="#三、缓存穿透-请求的数据本来不存在" aria-label="Permalink to &quot;三、缓存穿透：请求的数据本来不存在&quot;">​</a></h2><p>缓存穿透指请求查询的数据在缓存中不存在，在数据库中也不存在。因为查不到数据，系统通常不会写缓存，导致同样的无效请求或大量随机请求每次都绕过缓存访问数据库。它常见于恶意扫描随机 ID、接口参数缺少边界、爬虫批量探测、历史数据被删除但外部仍持续访问等场景。</p><p>诊断穿透时，要看两个信号：第一，数据库查询结果大量为空；第二，key 分布很离散，不能被少数热点解释。如果日志里出现大量不存在的用户 ID、商品 ID、订单 ID，并且这些 ID 的格式、范围、来源明显异常，就不能只当成缓存未命中处理。</p><h3 id="_1-参数校验-把明显非法请求挡在缓存之前" tabindex="-1">1. 参数校验：把明显非法请求挡在缓存之前 <a class="header-anchor" href="#_1-参数校验-把明显非法请求挡在缓存之前" aria-label="Permalink to &quot;1\\. 参数校验：把明显非法请求挡在缓存之前&quot;">​</a></h3><p>参数校验是最靠前、成本最低的治理方式。比如 ID 必须为正整数，页码不能超过合理范围，枚举值必须在白名单内，租户 ID 和用户权限要匹配，业务前缀必须合法。对于明显非法的请求，应该在 Controller、网关或业务入口直接拒绝，而不是进入缓存和数据库链路。</p><p>它的边界是只能挡住“格式非法”和“明显越界”的请求，挡不住格式合法但业务上不存在的 ID。因此参数校验通常是第一道防线，而不是完整方案。</p><h3 id="_2-布隆过滤器-先判断数据是否可能存在" tabindex="-1">2. 布隆过滤器：先判断数据是否可能存在 <a class="header-anchor" href="#_2-布隆过滤器-先判断数据是否可能存在" aria-label="Permalink to &quot;2\\. 布隆过滤器：先判断数据是否可能存在&quot;">​</a></h3><p>布隆过滤器适合存放“合法 ID 集合”或“存量数据集合”的存在性判断。请求进来后，先问布隆过滤器：这个 key 是否可能存在。如果判断一定不存在，就直接返回空结果或拒绝；如果判断可能存在，再继续查缓存和数据库。</p><p>布隆过滤器的优势是空间效率高，适合大规模 ID 集合。它的代价是存在误判：可能把不存在的数据判断为“可能存在”，所以仍然会有少量请求进入数据库；但它不会把已经加入的数据判断为“一定不存在”。另外，普通布隆过滤器不适合删除元素，如果业务有频繁删除，可以考虑重建过滤器、分层过滤器、计数布隆过滤器，或通过数据库最终兜底。</p><p>工程落地时还要处理两个问题：一是启动时如何加载过滤器，避免服务启动后过滤器为空；二是新增数据如何同步写入过滤器，避免新数据被误挡。比较稳的做法是初始化全量构建，增量写入时和数据库事务或消息事件联动，并监控过滤器命中率和误判带来的数据库空查比例。</p><h3 id="_3-空值缓存-把查不到的结果也短暂缓存" tabindex="-1">3. 空值缓存：把查不到的结果也短暂缓存 <a class="header-anchor" href="#_3-空值缓存-把查不到的结果也短暂缓存" aria-label="Permalink to &quot;3\\. 空值缓存：把查不到的结果也短暂缓存&quot;">​</a></h3><p>空值缓存是指数据库查询为空时，也在 Redis 写入一个空结果，并设置较短 TTL。这样同一个不存在 key 的重复请求就会被缓存挡住。它非常适合“同一批不存在 ID 被频繁访问”的场景，例如旧链接、删除后的商品详情、重复探测的用户 ID。</p><p>空值缓存的主要代价是内存污染。如果攻击者每次都构造随机 key，空值缓存会把大量无意义 key 写入 Redis，反而让缓存层承压。因此空值缓存要配合短 TTL、参数校验、限流和风控使用。还有一个常见边界是数据后来被创建：如果某个 ID 之前查不到并写了空值缓存，后续创建了真实数据，必须删除空值缓存或让空值 TTL 足够短，否则用户会短暂读到“数据不存在”。</p><h3 id="_4-风控和限流-处理恶意穿透流量" tabindex="-1">4. 风控和限流：处理恶意穿透流量 <a class="header-anchor" href="#_4-风控和限流-处理恶意穿透流量" aria-label="Permalink to &quot;4\\. 风控和限流：处理恶意穿透流量&quot;">​</a></h3><p>如果穿透来自恶意扫描，单靠缓存方案不够。系统需要结合 IP、用户、设备、接口、参数模式做风控限流。比如同一 IP 在短时间内访问大量随机 ID，可以触发验证码、黑名单、限速或直接拒绝；同一用户频繁查询不属于自己的资源，应优先走权限校验和审计。</p><p>这一层的代价是可能误伤正常用户，所以要保留灰度、白名单、告警和人工处置通道。面试中可以强调：穿透既是缓存问题，也是流量治理问题，不能把恶意请求全部交给 Redis 和数据库承受。</p><h2 id="四、缓存雪崩-大面积失效或缓存层不可用" tabindex="-1">四、缓存雪崩：大面积失效或缓存层不可用 <a class="header-anchor" href="#四、缓存雪崩-大面积失效或缓存层不可用" aria-label="Permalink to &quot;四、缓存雪崩：大面积失效或缓存层不可用&quot;">​</a></h2><p>缓存雪崩指大量 key 在同一时间失效，或者 Redis 整体不可用，导致请求集中打到底层数据库或下游服务。它和击穿最大的区别是影响范围：击穿通常是单个热点 key，雪崩是成批 key 或整个缓存层。真实项目里，雪崩往往出现在定时批量写缓存、服务重启集中预热、活动数据统一设置 TTL、Redis 集群故障、网络隔离、内存打满淘汰异常等场景。</p><h3 id="_1-ttl-随机-避免同一批-key-同时过期" tabindex="-1">1. TTL 随机：避免同一批 key 同时过期 <a class="header-anchor" href="#_1-ttl-随机-避免同一批-key-同时过期" aria-label="Permalink to &quot;1\\. TTL 随机：避免同一批 key 同时过期&quot;">​</a></h3><p>如果一批缓存用同一个任务写入，并设置完全相同的 TTL，那么它们很可能在同一秒或同一分钟同时过期。治理方式是在基础 TTL 上增加随机偏移，例如 baseTtl + random(0, 10min)，让过期时间自然打散。</p><p>TTL 随机的代价是数据过期时间不完全一致，某些 key 会比预期更早或更晚刷新。如果业务要求非常严格的过期时间，比如优惠券有效期、权限变更结果，就不能只靠随机 TTL，需要结合主动删除、版本号或逻辑校验。</p><h3 id="_2-缓存预热-让核心数据在流量前就位" tabindex="-1">2. 缓存预热：让核心数据在流量前就位 <a class="header-anchor" href="#_2-缓存预热-让核心数据在流量前就位" aria-label="Permalink to &quot;2\\. 缓存预热：让核心数据在流量前就位&quot;">​</a></h3><p>雪崩治理中的预热，重点不是只预热一个热点 key，而是把活动页、首页、配置、榜单、商品池等核心数据按优先级提前加载。预热任务要控制并发和速率，避免在流量还没来之前先把数据库打满。</p><p>更稳的预热流程是：先根据业务活动和历史访问日志确定预热清单，再按分片分页加载数据，然后写入 Redis 并设置随机 TTL，最后校验命中率、key 数量、内存占用和失败重试情况。预热完成后还要保留降级开关，因为预热成功不代表运行期间不会发生 Redis 故障。</p><h3 id="_3-多级缓存-不要让-redis-成为唯一屏障" tabindex="-1">3. 多级缓存：不要让 Redis 成为唯一屏障 <a class="header-anchor" href="#_3-多级缓存-不要让-redis-成为唯一屏障" aria-label="Permalink to &quot;3\\. 多级缓存：不要让 Redis 成为唯一屏障&quot;">​</a></h3><p>多级缓存通常包括浏览器/客户端缓存、CDN、网关缓存、本地缓存、Redis 缓存和数据库。对于读多写少、允许短暂旧数据的内容，本地缓存和 CDN 能显著降低 Redis 压力。比如首页配置可以在应用内保留几秒到几十秒的 local cache，Redis 抖动时仍能返回最近一次可用数据。</p><p>多级缓存的代价是失效链路更复杂，数据一致性更难。使用 local cache 时，要明确最大可接受旧数据时间，并设计主动刷新、版本号、消息通知或短 TTL。不要在强一致业务里随意加多级缓存，否则雪崩没发生，数据错误先发生。</p><h3 id="_4-限流降级-保护数据库和核心链路" tabindex="-1">4. 限流降级：保护数据库和核心链路 <a class="header-anchor" href="#_4-限流降级-保护数据库和核心链路" aria-label="Permalink to &quot;4\\. 限流降级：保护数据库和核心链路&quot;">​</a></h3><p>当缓存大面积失效时，系统必须限制回源流量。常见方式包括接口级限流、用户级限流、热点资源限流、连接池隔离、熔断下游服务、返回兜底数据、关闭非核心功能。比如商品详情页可以先返回基础信息和“库存繁忙”，推荐模块可以降级为空，排行榜可以返回上一版本快照。</p><p>限流降级的代价是用户体验下降，但它能保护核心链路不被拖垮。工程上要提前定义降级数据来源和开关，而不是事故发生后临时写逻辑。面试表达时可以说：雪崩治理的目标不是保证所有请求都成功，而是让核心服务在缓存层失效时仍可控。</p><h3 id="_5-redis-整体不可用时的兜底" tabindex="-1">5. Redis 整体不可用时的兜底 <a class="header-anchor" href="#_5-redis-整体不可用时的兜底" aria-label="Permalink to &quot;5\\. Redis 整体不可用时的兜底&quot;">​</a></h3><p>如果 Redis 集群整体不可用，普通的 key 级治理已经无效。此时要从系统层面兜底：</p><table tabindex="0"><thead><tr><th>兜底措施</th><th>作用</th><th>代价和边界</th></tr></thead><tbody><tr><td>本地缓存快照</td><td>Redis 不可用时返回最近一次成功数据</td><td>只能用于允许短暂旧数据的场景</td></tr><tr><td>读请求限流</td><td>控制进入数据库的请求量</td><td>会拒绝部分正常请求</td></tr><tr><td>数据库连接池隔离</td><td>防止缓存故障拖垮全部数据库连接</td><td>需要提前按业务拆分资源</td></tr><tr><td>熔断降级</td><td>Redis 或数据库异常时快速失败</td><td>需要定义可接受的降级结果</td></tr><tr><td>核心链路兜底页</td><td>保住活动页、详情页、配置页基本可用</td><td>信息可能不完整或不实时</td></tr><tr><td>故障开关</td><td>临时关闭非核心模块和重型查询</td><td>需要运维、研发、业务共同演练</td></tr></tbody></table><p>这里最容易被忽略的是“数据库保护”。Redis 不可用后，如果所有应用实例都直接查库，数据库连接池会被迅速占满，最终连写请求和核心查询都受影响。因此缓存故障预案一定要包含数据库限流、连接池隔离和核心 SQL 保护。</p><h2 id="五、bigkey-与-hotkey-不是同一个问题" tabindex="-1">五、BigKey 与 HotKey：不是同一个问题 <a class="header-anchor" href="#五、bigkey-与-hotkey-不是同一个问题" aria-label="Permalink to &quot;五、BigKey 与 HotKey：不是同一个问题&quot;">​</a></h2><p>BigKey 关注的是单个 key 的体积或元素数量过大，HotKey 关注的是单个 key 的访问频率过高。一个 key 可以很大但不热，也可以很热但很小；如果又大又热，风险最高。治理前要先区分维度，否则容易把“拆分体积”和“分散读压力”混在一起。</p><p><img src="'+l+'" alt="images/redis-19-BigKey与HotKey治理.png"></p><p>images/redis-19-BigKey与HotKey治理.png</p><h3 id="_1-bigkey-的定义和危害" tabindex="-1">1. BigKey 的定义和危害 <a class="header-anchor" href="#_1-bigkey-的定义和危害" aria-label="Permalink to &quot;1\\. BigKey 的定义和危害&quot;">​</a></h3><p>BigKey 没有全行业统一阈值，常见经验是：String value 超过 1MB 可以重点关注，Hash/List/Set/ZSet 元素达到几千到几万级也要警惕。真正的判断标准要结合实例规格、网络带宽、命令耗时、复制延迟和业务响应时间。</p><p>BigKey 的危害主要有五类：</p><ol><li>网络传输大。一次 GET 或集合全量读取会占用大量带宽，客户端反序列化也会变慢。</li><li>主线程阻塞。Redis 单线程执行命令，大对象读写、删除、编码转换都可能拉长事件循环。</li><li>内存倾斜。Cluster 中某个分片因为少数大 key 内存占用过高，导致迁移、扩容和淘汰压力集中。</li><li>主从复制慢。大 key 写入、删除或频繁更新会放大复制缓冲区压力，增加主从延迟。</li><li>删除风险高。直接 DEL 大集合可能阻塞主线程，造成请求抖动。</li></ol><h3 id="_2-bigkey-怎么发现" tabindex="-1">2. BigKey 怎么发现 <a class="header-anchor" href="#_2-bigkey-怎么发现" aria-label="Permalink to &quot;2\\. BigKey 怎么发现&quot;">​</a></h3><p>BigKey 可以从离线扫描、线上采样和监控告警三个层面发现：</p><table tabindex="0"><thead><tr><th>方法</th><th>适用场景</th><th>边界</th></tr></thead><tbody><tr><td>redis-cli --bigkeys</td><td>快速扫描各类型最大 key</td><td>基于 SCAN 抽样遍历，只给出大小线索，不代表完整内存占用</td></tr><tr><td>MEMORY USAGE key</td><td>精确查看某个 key 的内存占用</td><td>需要已知 key，频繁执行也会增加实例压力</td></tr><tr><td>SCAN + 类型判断</td><td>自定义批量巡检 key</td><td>不能用 KEYS * 在线上全量阻塞扫描</td></tr><tr><td>监控内存倾斜</td><td>发现某个分片内存异常</td><td>只能提示方向，需要继续定位具体 key</td></tr><tr><td>慢日志和网络流量</td><td>发现大对象读写导致的慢请求</td><td>只能看到执行慢的命令，不一定覆盖所有大 key</td></tr></tbody></table><p>线上排查时，SCAN 要控制 COUNT，并在低峰执行；--bigkeys 也不是完全无成本，不能在业务高峰频繁跑。对于集合类型，不要用 HGETALL、LRANGE 0 -1、SMEMBERS、ZRANGE 0 -1 做排查，它们本身就可能制造慢请求。</p><h3 id="_3-bigkey-怎么治理" tabindex="-1">3. BigKey 怎么治理 <a class="header-anchor" href="#_3-bigkey-怎么治理" aria-label="Permalink to &quot;3\\. BigKey 怎么治理&quot;">​</a></h3><p>BigKey 的核心治理是拆分和限界。设计阶段就要避免把无限增长的数据塞进一个 key，例如把用户所有消息、商品所有评论、活动所有参与记录都放在一个集合里。更合理的方式是按用户、业务、时间、分页、分片号拆分，例如 comment:{itemId}:{pageNo}、rank:{date}:{bucket}、user:msg:{uid}:{month}。</p><p>对于已经存在的 BigKey，要先确认它的读写模式，再选择治理方式：</p><ol><li>如果是大 String，可以拆成多个字段或按版本、分片拆分，避免一次读取全部内容。</li><li>如果是大 Hash，可以按字段范围、业务维度或哈希桶拆成多个小 Hash。</li><li>如果是大 List/ZSet，可以改成分页 key，或把历史数据沉到数据库、ES、对象存储。</li><li>如果只是删除风险高，优先用 UNLINK，让 Redis 把释放内存的工作交给后台线程处理。</li><li>如果必须逐步清理集合元素，使用 HSCAN、SSCAN、ZSCAN 或分批 LPOP/RPOP，不要一次性全量删除。</li></ol><p>UNLINK 和 lazyfree 相关配置需要一起理解。UNLINK 是用户显式异步删除 key，能减少大 key 删除对主线程的阻塞；lazyfree-lazy-user-del 可以让普通 DEL 也尽量走异步释放；lazyfree-lazy-expire、lazyfree-lazy-eviction、lazyfree-lazy-server-del 分别影响过期、淘汰和服务内部删除的异步释放行为。它们降低的是释放内存阶段的阻塞，不等于大 key 读写、网络传输、复制压力都消失，所以 BigKey 的根本方案仍然是拆分和限制增长。</p><h3 id="_4-hotkey-的定义和发现" tabindex="-1">4. HotKey 的定义和发现 <a class="header-anchor" href="#_4-hotkey-的定义和发现" aria-label="Permalink to &quot;4\\. HotKey 的定义和发现&quot;">​</a></h3><p>HotKey 是访问频率异常高的 key。它不一定占用很多内存，但会把 Redis 单线程、某个 Cluster 分片、网卡或客户端连接池打满。热点商品详情、热门配置、直播间状态、秒杀库存、爆款榜单都可能成为 HotKey。</p><p>HotKey 的发现更依赖流量侧数据。可以从以下位置入手：</p><table tabindex="0"><thead><tr><th>位置</th><th>方法</th><th>适用性</th></tr></thead><tbody><tr><td>客户端</td><td>SDK 埋点统计 key 访问频次</td><td>最准确，能关联业务接口和用户请求</td></tr><tr><td>网关/代理层</td><td>采样 Redis 请求或业务请求参数</td><td>适合统一入口，但要处理采样误差</td></tr><tr><td>Redis 侧</td><td>--hotkeys、命令统计、CPU、网络流量</td><td>依赖 LFU 策略或实例指标，适合辅助判断</td></tr><tr><td>业务日志</td><td>统计商品 ID、活动 ID、配置 ID</td><td>能最快定位业务含义</td></tr></tbody></table><p>redis-cli --hotkeys 依赖 Redis 的 LFU 访问频率信息，通常需要淘汰策略使用 LFU 相关策略才有更好的参考价值。它适合辅助发现热点，不适合作为唯一依据。MONITOR 能看到实例上的所有命令，但开销很高，会显著影响性能，只适合短时间、低峰、精确问题复现时使用，线上高峰不要长时间打开。</p><h3 id="_5-hotkey-怎么治理" tabindex="-1">5. HotKey 怎么治理 <a class="header-anchor" href="#_5-hotkey-怎么治理" aria-label="Permalink to &quot;5\\. HotKey 怎么治理&quot;">​</a></h3><p>HotKey 的治理目标是分散读压力和限制异常流量：</p><ol><li>本地缓存。把极热点、读多写少、允许短暂旧数据的内容缓存在 JVM 内存中，例如 Caffeine。代价是多节点一致性变弱，需要短 TTL、消息通知或版本号控制。</li><li>副本 key。把同一份热点数据复制成多个 key，例如 item💯copy:0..N，请求随机读取其中一个。代价是写入和失效要同步多个副本，适合读多写少。</li><li>读写分离。对于读多写少场景，可以让从节点承担读流量。代价是主从复制有延迟，不能用于要求强一致的读。</li><li>限流降级。突发热点超过系统承载时，对接口、用户、资源维度限流，或者返回兜底数据。代价是部分请求体验下降，但能保护 Redis 和数据库。</li><li>热点预热。活动开始前把热点数据加载到 Redis 和 local cache，避免冷启动瞬间集中回源。</li></ol><p>如果 HotKey 同时也是 BigKey，要先拆 BigKey，再谈读扩散。否则即使增加本地缓存或副本 key，每次更新和复制仍然会带来大对象成本。</p><h2 id="六、缓存预热-从-加载数据-升级为-上线流程" tabindex="-1">六、缓存预热：从“加载数据”升级为“上线流程” <a class="header-anchor" href="#六、缓存预热-从-加载数据-升级为-上线流程" aria-label="Permalink to &quot;六、缓存预热：从“加载数据”升级为“上线流程”&quot;">​</a></h2><p>缓存预热是把确定会被大量访问的数据，在流量到来之前加载到缓存。它常用于大促活动、直播带货、课程详情、首页推荐、配置中心、榜单和热点商品。一个成熟的预热流程通常包括：</p><ol><li>识别预热对象。来源可以是活动配置、运营圈选、历史访问 TopN、搜索热词、实时趋势和人工白名单。</li><li>计算预热范围。区分 P0 核心数据、P1 高频数据、P2 可延迟加载数据，避免把全量数据库搬进 Redis。</li><li>分片加载。按业务 ID、分页、时间片或分片号批量加载，控制并发和 QPS。</li><li>写入缓存。设置合理 TTL，并对可过期数据增加随机偏移；对核心热点可以使用逻辑过期。</li><li>校验结果。检查 key 数量、命中率、内存占用、失败列表、加载耗时和数据库压力。</li><li>运行期刷新。通过定时任务、消息事件或后台刷新维持热点数据，而不是只在启动时加载一次。</li><li>应急回滚。如果预热数据错误或占用过高，要能按活动、版本或 key 前缀快速清理。</li></ol><p>预热最常见的问题是“启动即预热”。如果每个应用节点启动时都加载一遍同样的数据，会造成重复查库和重复写 Redis。更稳的方式是交给任务调度平台统一执行，或用分布式锁保证同一批预热任务只有一个执行者。预热任务还要做幂等，避免失败重试时写入脏版本。</p><h2 id="七、常用排查命令和边界" tabindex="-1">七、常用排查命令和边界 <a class="header-anchor" href="#七、常用排查命令和边界" aria-label="Permalink to &quot;七、常用排查命令和边界&quot;">​</a></h2><p>Redis 排查命令不能只会背名称，还要知道它们的使用边界。很多命令本身也会消耗实例资源，在故障高峰误用会放大问题。</p><table tabindex="0"><thead><tr><th>命令或工具</th><th>主要用途</th><th>使用边界</th></tr></thead><tbody><tr><td>SLOWLOG GET</td><td>查看执行时间超过阈值的慢命令</td><td>只能看到命令执行慢，不包含网络传输和客户端处理时间</td></tr><tr><td>redis-cli --bigkeys</td><td>扫描各类型较大的 key</td><td>基于遍历，线上高峰慎用，结果偏“最大元素数”而非完整诊断</td></tr><tr><td>MEMORY USAGE key</td><td>查看指定 key 内存占用</td><td>适合确认已怀疑的 key，不适合对未知 key 高频全量执行</td></tr><tr><td>SCAN cursor MATCH pattern COUNT n</td><td>渐进式遍历 key</td><td>不保证一次返回固定数量，不能替代索引查询，仍需控制频率</td></tr><tr><td>HSCAN/SSCAN/ZSCAN</td><td>渐进式遍历集合内部元素</td><td>适合分批治理集合，不能解决集合本身过大的设计问题</td></tr><tr><td>MONITOR</td><td>实时观察所有命令</td><td>开销很高，只能短时间低峰使用，生产高峰长开很危险</td></tr><tr><td>redis-cli --hotkeys</td><td>辅助发现热点 key</td><td>依赖 LFU 频率信息，更适合作为辅助线索</td></tr><tr><td>INFO commandstats</td><td>查看命令调用次数和耗时</td><td>只能到命令维度，不能直接定位具体业务 key</td></tr><tr><td>INFO memory</td><td>查看内存、碎片率、淘汰等指标</td><td>只能说明实例状态，需要结合 key 维度排查</td></tr><tr><td>LATENCY DOCTOR</td><td>分析 Redis 内部延迟事件</td><td>依赖延迟监控配置，适合辅助判断阻塞类型</td></tr></tbody></table><p>工程排查可以按一条链路展开：先看应用接口耗时和错误率，再看缓存命中率和 Redis 实例指标，然后用慢日志、命令统计、key 采样去缩小范围，最后定位到具体 key、具体接口和具体业务动作。不要一上来在线上执行 KEYS *、全量 HGETALL 或长时间 MONITOR，这些动作本身就可能成为事故的一部分。</p><h2 id="八、常见问题和落地排查步骤" tabindex="-1">八、常见问题和落地排查步骤 <a class="header-anchor" href="#八、常见问题和落地排查步骤" aria-label="Permalink to &quot;八、常见问题和落地排查步骤&quot;">​</a></h2><h3 id="_1-数据库-qps-突然升高" tabindex="-1">1. 数据库 QPS 突然升高 <a class="header-anchor" href="#_1-数据库-qps-突然升高" aria-label="Permalink to &quot;1\\. 数据库 QPS 突然升高&quot;">​</a></h3><p>先确认是不是缓存命中率下降。如果命中率下降同时集中在少数 key，看缓存击穿和 HotKey；如果 miss key 很分散且数据库返回空值多，看缓存穿透；如果大量 key TTL 接近或 Redis 有故障告警，看缓存雪崩。接着检查最近是否有发布、缓存清理、活动上线、批量刷新和配置变更。</p><h3 id="_2-redis-cpu-很高但数据库压力不高" tabindex="-1">2. Redis CPU 很高但数据库压力不高 <a class="header-anchor" href="#_2-redis-cpu-很高但数据库压力不高" aria-label="Permalink to &quot;2\\. Redis CPU 很高但数据库压力不高&quot;">​</a></h3><p>这种情况更像 Redis 自身压力过大。优先看慢日志、命令统计、网络流量和 key 大小，判断是否存在大集合全量读取、BigKey 删除、HotKey 高频访问、Lua 脚本耗时、短连接过多等问题。治理方向不一定是加缓存，而是拆 key、改命令、限流、连接池复用和读扩散。</p><h3 id="_3-缓存命中率很高但接口仍然慢" tabindex="-1">3. 缓存命中率很高但接口仍然慢 <a class="header-anchor" href="#_3-缓存命中率很高但接口仍然慢" aria-label="Permalink to &quot;3\\. 缓存命中率很高但接口仍然慢&quot;">​</a></h3><p>命中率高不代表响应一定快。可能是命中的 value 太大，网络传输和反序列化慢；也可能是 Redis 分片热点、客户端连接池等待、本地 JSON 解析慢、下游组装逻辑慢。此时要看 P99 延迟、value 大小、客户端耗时分段，而不是只看 Redis 是否命中。</p><h3 id="_4-空值缓存和布隆过滤器怎么选" tabindex="-1">4. 空值缓存和布隆过滤器怎么选 <a class="header-anchor" href="#_4-空值缓存和布隆过滤器怎么选" aria-label="Permalink to &quot;4\\. 空值缓存和布隆过滤器怎么选&quot;">​</a></h3><p>如果非法请求集中在少量不存在 key 上，空值缓存很有效；如果非法请求是大规模随机 ID，布隆过滤器和风控更重要。实际项目通常组合使用：参数校验挡明显非法，布隆过滤器挡大部分不存在 ID，数据库空查结果用短 TTL 空值缓存吸收重复访问，异常来源再做限流和风控。</p><h3 id="_5-本地缓存会不会导致数据不一致" tabindex="-1">5. 本地缓存会不会导致数据不一致 <a class="header-anchor" href="#_5-本地缓存会不会导致数据不一致" aria-label="Permalink to &quot;5\\. 本地缓存会不会导致数据不一致&quot;">​</a></h3><p>会，所以要限制使用范围。local cache 适合读多写少、允许短暂旧数据的热点内容。可以用短 TTL、版本号、消息通知、主动刷新来降低不一致窗口。面试里不要把本地缓存描述成无代价方案，最好主动说明它牺牲了一部分实时性来换取 Redis 降压。</p><h2 id="九、项目面试表达" tabindex="-1">九、项目面试表达 <a class="header-anchor" href="#九、项目面试表达" aria-label="Permalink to &quot;九、项目面试表达&quot;">​</a></h2><p>回答缓存治理类问题时，可以按“现象 -&gt; 定位 -&gt; 方案 -&gt; 代价 -&gt; 项目落地”组织。一个完整表达可以这样说：</p><p>我不会直接把缓存问题都归为击穿，而是先看 miss key 的分布和数据库返回结果。如果是单个热点 key 过期导致并发回源，我会用互斥锁或逻辑过期保护数据库，并在活动前做热点预热；如果是大量不存在 ID，我会先做参数校验，再用布隆过滤器和空值缓存，恶意流量还要加风控限流；如果是大量 key 同时失效或 Redis 整体故障，就要打散 TTL、做多级缓存和限流降级，同时准备 Redis 不可用时的本地快照和数据库保护。</p><p>如果面试官继续追问 BigKey 和 HotKey，可以补充：</p><p>BigKey 和 HotKey 是两个维度。BigKey 是 value 或集合元素过大，风险在网络传输、主线程阻塞、复制延迟和删除阻塞，治理上要拆分结构、限制集合规模，删除时用 UNLINK 或分批删除，并结合 lazyfree 降低释放内存的阻塞。HotKey 是访问频率过高，风险在单分片、单线程或网卡被打满，治理上用本地缓存、副本 key、读写分离、限流降级和预热。又大又热的 key 要优先拆分，否则读扩散也会复制大对象成本。</p><p>项目复盘时还可以强调排查命令的边界：</p><p>排查 Redis 问题时，我会先看应用指标、缓存命中率、Redis CPU、内存、网络和慢日志，再用 SCAN、MEMORY USAGE、--bigkeys、命令统计去缩小范围。MONITOR 和 --hotkeys 只能作为短时间辅助工具，不能在高峰长期打开。对于线上大 key，不能用 KEYS * 或全量集合读取去排查，否则排查动作本身可能造成阻塞。</p><p>准备到这个程度，缓存问题就不只是名词题，而能回答到工程治理：如何定位问题，为什么选择某个方案，方案牺牲了什么，以及事故发生时怎样保护数据库和核心业务链路。</p>',115)])])}const u=t(h,[["render",o]]);export{b as __pageData,u as default};
