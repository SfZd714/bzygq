import{_ as e,o as a,c as r,a2 as o}from"./chunks/framework.F7eQ-ohH.js";const c=JSON.parse('{"title":"05 Broker存储、高性能读写与日志保留","description":"","frontmatter":{},"headers":[],"relativePath":"Java面试问题汇总/Kafka/05 Broker存储、高性能读写与日志保留.md","filePath":"Java面试问题汇总/Kafka/05 Broker存储、高性能读写与日志保留.md","lastUpdated":1788929995000}'),d={name:"Java面试问题汇总/Kafka/05 Broker存储、高性能读写与日志保留.md"};function i(n,t,l,p,h,k){return a(),r("div",null,[...t[0]||(t[0]=[o('<h1 id="_05-broker存储、高性能读写与日志保留" tabindex="-1">05 Broker存储、高性能读写与日志保留 <a class="header-anchor" href="#_05-broker存储、高性能读写与日志保留" aria-label="Permalink to &quot;05 Broker存储、高性能读写与日志保留&quot;">​</a></h1><p>Kafka Broker 是 Kafka 集群中负责接收、持久化、复制和对外提供消息读取的服务节点。Producer 最终把消息发送到某个 Topic-Partition 的 Leader Broker，Consumer 也从对应分区的 Leader 拉取消息；Follower Broker 则通过复制链路从 Leader 同步数据。理解 Broker 的关键不只是知道“一台 Kafka 机器叫 Broker”，而是要把它看成一个围绕日志文件构建的高吞吐存储系统：写入以追加日志为主，读取以 Offset 定位和顺序扫描为主，可靠性依赖副本和确认语义，容量治理依赖保留策略和日志清理。</p><p>传统数据库通常围绕行记录、索引更新、事务隔离和随机读写设计。Kafka 的目标不同，它更像一个可持久化的分布式提交日志：消息进入分区后被追加到日志末尾，消费者按自己的进度读取，不会因为某个消费者已经处理过就立即删除。也正因为这个模型，Kafka 能够同时获得高吞吐、可回放、可横向扩展和较强的故障恢复能力。</p><h2 id="一、broker在集群链路中的位置" tabindex="-1">一、Broker在集群链路中的位置 <a class="header-anchor" href="#一、broker在集群链路中的位置" aria-label="Permalink to &quot;一、Broker在集群链路中的位置&quot;">​</a></h2><p>从数据链路看，Broker 同时处在写入路径、复制路径和读取路径上。Producer 发送消息前会根据元数据找到目标分区的 Leader；Leader Broker 接收请求后，把消息追加到本地日志，并根据 acks、ISR 和副本同步状态决定何时返回成功；Follower Broker 不直接处理普通生产写入，而是从 Leader 拉取日志并追赶进度；Consumer 拉取消息时，也会基于 Topic、Partition 和 Offset 从 Leader 读取。</p><p>这条链路里有几个容易混淆的点：</p><table tabindex="0"><thead><tr><th>角色</th><th>主要职责</th><th>常见误区</th></tr></thead><tbody><tr><td>Broker</td><td>承载分区副本、处理读写请求、维护本地日志文件</td><td>Broker 不是单纯的网络转发节点，它同时承担持久化存储</td></tr><tr><td>Partition Leader</td><td>处理该分区的生产写入和消费读取</td><td>一个 Topic 的不同分区可以分布在不同 Broker 上</td></tr><tr><td>Partition Follower</td><td>从 Leader 同步日志，参与容灾</td><td>Follower 通常不服务普通客户端读写</td></tr><tr><td>Controller</td><td>管理分区 Leader、Broker 状态和集群元数据变更</td><td>Controller 属于控制平面，不是每条消息的数据路径</td></tr></tbody></table><p>原始文章中提到 Kafka 依赖 ZooKeeper 管理集群信息和 Leader 选举，这是早期 Kafka 的典型部署方式。新版本 Kafka 已经逐步转向 KRaft 模式，把元数据管理收敛到 Kafka 自身的控制器仲裁层中。学习时要抓住本质：Kafka 必须有一个控制平面来维护 Broker、Topic、Partition、Replica、Leader 和 ISR 等元数据，客户端和 Broker 的数据读写则依赖这些元数据找到正确的分区副本。</p><h2 id="二、分区日志不是一个无限增长的大文件" tabindex="-1">二、分区日志不是一个无限增长的大文件 <a class="header-anchor" href="#二、分区日志不是一个无限增长的大文件" aria-label="Permalink to &quot;二、分区日志不是一个无限增长的大文件&quot;">​</a></h2><p>Kafka 的存储单位是 Topic 下的 Partition。每个 Partition 在 Broker 本地对应一组日志文件，逻辑上是一条有序追加的日志，物理上会被拆成多个 Log Segment。新消息总是写入当前活跃 Segment 的末尾，当 Segment 达到大小阈值、时间阈值，或者触发日志滚动条件时，Kafka 会关闭旧 Segment 并创建新的活跃 Segment。</p><p>一个典型的分区目录中会出现三类文件：</p><table tabindex="0"><thead><tr><th>文件类型</th><th>作用</th><th>典型含义</th></tr></thead><tbody><tr><td>.log</td><td>保存真正的消息批次数据</td><td>消息内容、Offset、时间戳、Header、压缩批次等</td></tr><tr><td>.index</td><td>Offset 到物理位置的稀疏索引</td><td>用于快速定位某个 Offset 附近的日志位置</td></tr><tr><td>.timeindex</td><td>时间戳到 Offset 的稀疏索引</td><td>用于按时间查找 Offset，例如从某个时间点回放</td></tr></tbody></table><p>Segment 文件名通常使用该 Segment 的起始 Offset。假设某个 Segment 文件名为 00000000000000100000.log，它表示这个文件中的第一条消息逻辑 Offset 从 100000 附近开始。这样命名的好处是定位时可以先根据文件名判断目标 Offset 落在哪个 Segment，再进入索引文件查找更精确的位置。</p><p>Segment 设计解决了三个工程问题。第一，日志清理可以按 Segment 粒度删除旧文件，而不是逐条删除消息。第二，索引文件可以控制在较小范围内，避免单个巨型日志文件带来过大的索引和恢复成本。第三，Broker 重启恢复时可以逐段扫描和校验，降低恢复时间和损坏影响范围。</p><h2 id="三、offset索引为什么做成稀疏索引" tabindex="-1">三、Offset索引为什么做成稀疏索引 <a class="header-anchor" href="#三、offset索引为什么做成稀疏索引" aria-label="Permalink to &quot;三、Offset索引为什么做成稀疏索引&quot;">​</a></h2><p>Kafka 的 Offset 是分区内的逻辑位置，不是文件系统中的物理字节地址。Consumer 说“我要从 offset=50000 开始读”，Broker 需要把这个逻辑位置转换成 .log 文件中的物理位置。这个转换依赖 .index 文件，但 Kafka 并不会给每一条消息都建一个完整索引，而是使用稀疏索引。</p><p>稀疏索引的基本流程是：</p><ol><li>根据目标 Offset 找到起始 Offset 不大于它的 Segment。</li><li>在该 Segment 的 Offset 索引中查找小于等于目标 Offset 的最近索引项。</li><li>拿到索引项对应的物理文件位置。</li><li>从该位置开始顺序扫描少量消息，直到找到目标 Offset。</li></ol><p>这种设计本质上是在写入成本和读取成本之间取平衡。每条消息都维护索引可以让定位更精确，但会显著增加写入放大、索引空间和刷盘压力；稀疏索引只记录部分位置，读取时多扫描一小段日志，换来更低的索引维护成本。对于 Kafka 这种以顺序追加和批量读取为主的系统来说，这个取舍非常合理。</p><p>时间索引用于另一个常见场景：按时间回放。比如线上排查希望从“昨天 10 点开始”重新消费某个 Topic，客户端可以通过 offsetsForTimes 查找指定时间附近的 Offset。Broker 会根据 .timeindex 找到接近该时间戳的 Offset，再回到 Offset 索引和日志文件中定位。要注意，时间索引给出的是接近位置，不是业务层严格等于某个时间点的边界，真实业务回放通常还需要在消费端根据消息时间或业务时间继续过滤。</p><h2 id="四、顺序追加是broker写入性能的基础" tabindex="-1">四、顺序追加是Broker写入性能的基础 <a class="header-anchor" href="#四、顺序追加是broker写入性能的基础" aria-label="Permalink to &quot;四、顺序追加是Broker写入性能的基础&quot;">​</a></h2><p>Kafka 高吞吐并不是因为它“不落盘”，而是因为它尽量把磁盘写入变成顺序追加。机械硬盘的随机写非常慢，主要损耗来自寻道和旋转延迟；顺序写则可以充分利用磁盘带宽。SSD 的随机性能比机械盘强，但顺序写仍然更容易获得稳定吞吐和更低写放大。</p><p>Broker 收到 Produce 请求后，并不是把每条消息当成一条数据库记录随机写入，而是把同一分区的消息批次追加到日志末尾。Producer 端的批量发送、压缩和 Broker 端的顺序日志结构是配套的：Producer 把多条消息合成一个批次，Broker 以批次为单位追加，Follower 也以批次为单位复制，Consumer 拉取时同样批量读取。</p><p>顺序追加还带来一个重要结果：Kafka 的写入性能与分区分布强相关。如果所有高流量消息都集中到一个分区，那么这个分区的 Leader 所在 Broker 会成为热点；如果 Topic 有合理的分区数，且分区 Leader 均匀分布在多个 Broker 上，写入压力就可以在集群中摊开。所谓 Kafka 可扩展，落到 Broker 层面就是“分区副本可以分散到多台机器，每台机器独立处理自己负责的分区日志”。</p><h2 id="五、page-cache让读写路径更接近操作系统能力" tabindex="-1">五、Page Cache让读写路径更接近操作系统能力 <a class="header-anchor" href="#五、page-cache让读写路径更接近操作系统能力" aria-label="Permalink to &quot;五、Page Cache让读写路径更接近操作系统能力&quot;">​</a></h2><p>原始文章提到 Producer 写入 Broker 后，消息会先进入 Page Cache，再由操作系统负责刷盘。更准确地说，Kafka 依赖操作系统的文件系统缓存来承接大量读写：应用把数据写入文件时，数据通常先进入内核 Page Cache，随后由操作系统根据脏页比例、刷盘周期和 I/O 调度策略异步写入磁盘。Kafka 不是完全绕开磁盘，而是把“何时把缓存页落到物理介质”交给操作系统和配置策略协调。</p><p>Page Cache 对 Kafka 有两类价值。</p><p>第一类是写入吸收。Broker 可以快速把数据追加到文件缓存中，不必每条消息都执行同步 fsync。如果每条消息都强制等待物理刷盘，吞吐会急剧下降。生产环境通常用副本同步、acks=all、min.insync.replicas 和禁用不干净选举来提高可靠性，而不是把所有可靠性都压在单机同步刷盘上。</p><p>第二类是读取命中。Kafka 中很多消息写入后很快就会被消费者读取，比如日志采集、指标上报、订单事件和实时计算链路。此时数据可能仍在 Page Cache 中，Consumer 拉取时不需要真正访问磁盘。即便是历史回放，只要访问模式接近顺序扫描，操作系统的预读和缓存机制也能发挥作用。</p><p>这里要避免一个简单化说法：Page Cache 不是 Kafka 独有技术，也不是“内存队列”。它是操作系统文件缓存。Kafka 的优势在于它的日志模型、批量读写和顺序访问模式非常适合 Page Cache 发挥作用。</p><h2 id="六、零拷贝优化的是从文件到网络的发送路径" tabindex="-1">六、零拷贝优化的是从文件到网络的发送路径 <a class="header-anchor" href="#六、零拷贝优化的是从文件到网络的发送路径" aria-label="Permalink to &quot;六、零拷贝优化的是从文件到网络的发送路径&quot;">​</a></h2><p>Consumer 从 Broker 拉取消息时，Broker 需要把日志文件中的数据发送到网络连接。传统路径可能经历多次数据复制：磁盘到内核缓冲区、内核缓冲区到用户态缓冲区、用户态缓冲区再到 Socket 缓冲区，期间还伴随用户态和内核态切换。Kafka 使用零拷贝相关能力时，可以减少这些不必要的用户态复制，让文件数据更直接地从内核文件缓存发送到网络。</p><p>面试或复盘时不要把“零拷贝”理解成完全没有复制。更准确的表述是：它减少了用户态和内核态之间的数据搬运，减少 CPU 拷贝和上下文切换，让 Broker 在高吞吐读取场景下把更多资源用于网络发送和请求处理。</p><p>顺序写、Page Cache、零拷贝之间不是孤立技巧，而是一组协同设计：</p><table tabindex="0"><thead><tr><th>机制</th><th>主要优化对象</th><th>典型效果</th></tr></thead><tbody><tr><td>批量追加写</td><td>写入路径</td><td>减少请求次数和随机写</td></tr><tr><td>Page Cache</td><td>读写缓存路径</td><td>利用操作系统缓存、刷盘和预读能力</td></tr><tr><td>零拷贝</td><td>文件到网络路径</td><td>降低 CPU 拷贝和上下文切换成本</td></tr><tr><td>稀疏索引</td><td>Offset 定位路径</td><td>在低索引成本下完成近似定位</td></tr></tbody></table><p>因此，Kafka 高性能的核心不是单点技巧，而是“日志追加模型 + 操作系统 I/O 能力 + 批量网络传输”的组合。</p><h2 id="七、写入成功语义由副本和确认条件共同决定" tabindex="-1">七、写入成功语义由副本和确认条件共同决定 <a class="header-anchor" href="#七、写入成功语义由副本和确认条件共同决定" aria-label="Permalink to &quot;七、写入成功语义由副本和确认条件共同决定&quot;">​</a></h2><p>Producer 收到成功响应，并不等于消息已经被每个副本永久写到物理磁盘；它表示 Broker 满足了当前配置下的确认条件。acks 是理解这件事的入口：</p><table tabindex="0"><thead><tr><th>参数</th><th>含义</th><th>风险与适用场景</th></tr></thead><tbody><tr><td>acks=0</td><td>Producer 不等待 Broker 确认</td><td>吞吐高，但发送失败不可感知，关键业务不建议使用</td></tr><tr><td>acks=1</td><td>Leader 写入本地后返回成功</td><td>Leader 宕机且 Follower 未同步时可能丢失</td></tr><tr><td>acks=all 或 acks=-1</td><td>Leader 等待 ISR 中满足条件的副本确认后返回</td><td>可靠性更高，延迟和可用性成本也更高</td></tr></tbody></table><p>min.insync.replicas 会进一步限制在 acks=all 下至少要有多少同步副本参与确认。例如副本因子为 3，min.insync.replicas=2，表示至少 Leader 加一个同步副本可用时才允许成功写入。如果 ISR 数量不足，Producer 会收到异常，而不是继续假装写入成功。</p><p>这也是 Kafka 可靠性语义的关键边界：它通常通过多副本复制抵抗单机故障，通过确认条件控制“成功”的门槛，通过保留策略支持后续回放；但如果配置成低确认、低副本、允许不干净选举，或者磁盘长期不可用，仍然可能出现数据丢失或不可用。</p><h2 id="八、日志保留决定消息生命周期" tabindex="-1">八、日志保留决定消息生命周期 <a class="header-anchor" href="#八、日志保留决定消息生命周期" aria-label="Permalink to &quot;八、日志保留决定消息生命周期&quot;">​</a></h2><p>Kafka 的消息被消费后不会立即删除。删除与否由 Topic 的日志保留配置决定，而不是由某个 Consumer 的处理进度决定。这一点和普通队列很不一样：Kafka 更像可回放日志，同一份消息可以被多个消费组独立读取；只要消息还在保留窗口内，新的消费组或重置 Offset 后的消费组都可以重新读取。</p><p>常见保留方式包括：</p><table tabindex="0"><thead><tr><th>策略</th><th>关键配置</th><th>适用场景</th></tr></thead><tbody><tr><td>按时间保留</td><td>retention.ms、retention.hours</td><td>日志、事件流、审计流水，保留最近 N 天</td></tr><tr><td>按大小保留</td><td>retention.bytes</td><td>磁盘容量明确受限，需要控制单个分区总大小</td></tr><tr><td>Segment 滚动</td><td>segment.bytes、segment.ms</td><td>控制日志文件切分粒度，影响清理和恢复效率</td></tr><tr><td>删除延迟</td><td>file.delete.delay.ms</td><td>Segment 标记删除后延迟物理删除，降低并发访问风险</td></tr></tbody></table><p>Kafka 清理旧数据时通常以 Segment 为单位。只有非活跃 Segment 满足过期或超量条件时，才会被删除。活跃 Segment 仍然承担写入，通常不会因为刚好有旧消息过期就被逐条切割。因此，保留时间不是精确到每条消息的秒级删除承诺，而是和 Segment 滚动、清理周期、文件状态共同作用的结果。</p><h2 id="九、log-compaction保留的是每个key的最新状态" tabindex="-1">九、Log Compaction保留的是每个Key的最新状态 <a class="header-anchor" href="#九、log-compaction保留的是每个key的最新状态" aria-label="Permalink to &quot;九、Log Compaction保留的是每个Key的最新状态&quot;">​</a></h2><p>Log Compaction 字面上容易被误解成“压缩算法”。实际上它是一种日志清理策略，目标不是减少字节编码，而是对相同 Key 的历史记录做状态折叠：对于同一个 Key，Kafka 尽量保留最新值，清理掉更早的旧值。它适用于需要保存“最新状态”的 Topic，例如用户资料快照、设备最新状态、配置变更、服务注册信息等。</p><p>普通删除策略和 Log Compaction 的差异可以这样理解：</p><table tabindex="0"><thead><tr><th>策略</th><th>保留目标</th><th>更适合的数据</th></tr></thead><tbody><tr><td>Delete</td><td>保留最近一段时间或一定大小内的完整事件流</td><td>订单事件、行为日志、指标明细、审计流水</td></tr><tr><td>Compact</td><td>尽量保留每个 Key 的最新值</td><td>用户状态、配置表、维表变更、设备状态</td></tr><tr><td>Delete + Compact</td><td>既压缩旧 Key，又限制总体保留窗口</td><td>状态流长期膨胀且仍需删除很旧数据</td></tr></tbody></table><p>Compaction 也不是立即生效的强一致清理。它由后台 cleaner 线程异步执行，且通常只处理非活跃 Segment。消费端仍然可能读到同一 Key 的多条历史记录，因此业务不能把 Compaction 当成“只会收到最新值”的读取保证。它保证的是日志长期存储趋向于保留最新状态，而不是改变消息到达时的事件语义。</p><p>在 Compaction Topic 中，带有 Key 且 Value 为 null 的消息通常被称为 tombstone，用来表示该 Key 的删除标记。tombstone 会保留一段时间，给下游消费者机会感知删除事件，之后再被清理。这个机制在构建缓存同步、维表更新和状态恢复时非常重要。</p><h2 id="十、controller与kraft维护的是控制平面" tabindex="-1">十、Controller与KRaft维护的是控制平面 <a class="header-anchor" href="#十、controller与kraft维护的是控制平面" aria-label="Permalink to &quot;十、Controller与KRaft维护的是控制平面&quot;">​</a></h2><p>Kafka 集群除了数据平面，还需要控制平面。数据平面负责 Produce、Fetch、Replica Fetch 等消息读写；控制平面负责维护哪些 Broker 存活、哪些 Topic 存在、每个分区有哪些副本、谁是 Leader、ISR 如何变化，以及 Broker 故障后如何重新选主。</p><p>早期 Kafka 使用 ZooKeeper 保存和协调元数据，Controller Broker 监听 ZooKeeper 上的变化并执行分区 Leader 选举、Broker 上下线处理等操作。KRaft 模式下，Kafka 使用内部的元数据日志和控制器仲裁机制来管理这些信息，减少对外部 ZooKeeper 集群的依赖。对学习和面试来说，重点不是死记“某版本一定用哪个组件”，而是能讲清控制平面要解决的问题：</p><ol><li>集群成员变化时，谁判断 Broker 上下线。</li><li>分区 Leader 失效时，谁选择新的 Leader。</li><li>Topic 创建、分区扩容、副本迁移时，元数据如何一致传播。</li><li>客户端元数据过期时，如何刷新并重新路由请求。</li></ol><p>很多线上异常表面看是 Producer 或 Consumer 报错，底层常常是元数据变化带来的短暂不一致。例如 Leader 刚迁移，客户端仍按旧元数据发送请求，就会遇到 NotLeaderOrFollower、LeaderNotAvailable、UnknownTopicOrPartition 等错误。成熟的客户端会刷新元数据并重试，业务侧要把这些异常纳入可重试范围，而不是简单判定为永久失败。</p><h2 id="十一、容量规划要同时算数据、复制和余量" tabindex="-1">十一、容量规划要同时算数据、复制和余量 <a class="header-anchor" href="#十一、容量规划要同时算数据、复制和余量" aria-label="Permalink to &quot;十一、容量规划要同时算数据、复制和余量&quot;">​</a></h2><p>Kafka 容量规划不能只看“每天写入多少原始消息”。Broker 需要承载原始数据、压缩后数据、索引文件、副本放大、日志滚动余量、重分配临时空间、操作系统空间，以及故障恢复期间的额外压力。一个简化估算公式可以写成：</p><p>总磁盘需求 ≈ 每日写入量 × 保留天数 × 副本因子 ÷ 压缩比 × 安全余量</p><p>这里的“压缩比”要按实际消息类型压测，不能凭感觉估计。日志文本、JSON 和重复字段较多的 IoT 数据通常压缩收益明显；已经压缩过的图片、二进制包、加密内容压缩收益可能很低。安全余量也不能省，生产集群通常要为 Segment 滚动、索引、Controller 元数据变化、分区重分配、Broker 下线后的流量倾斜和人工运维窗口预留空间。</p><p>容量规划还要关注分区层面的均衡。一个 Broker 总磁盘不满，不代表没有风险；如果某几个热点分区集中在同一块磁盘或同一台 Broker 上，仍然可能出现局部磁盘打满、I/O 延迟升高、复制滞后和消费延迟扩散。生产环境需要持续观察这些指标：</p><table tabindex="0"><thead><tr><th>维度</th><th>典型指标</th><th>含义</th></tr></thead><tbody><tr><td>磁盘容量</td><td>磁盘使用率、日志目录剩余空间</td><td>判断是否接近写满风险</td></tr><tr><td>写入速度</td><td>Topic/Partition 写入字节数、请求延迟</td><td>判断热点 Topic 和突增流量</td></tr><tr><td>副本健康</td><td>Under Replicated Partitions、Offline Partitions</td><td>判断副本是否追不上或不可用</td></tr><tr><td>清理压力</td><td>Log cleaner backlog、Segment 增长速度</td><td>判断 Compaction 或删除是否滞后</td></tr><tr><td>客户端延迟</td><td>Produce/Fetch 请求延迟、队列时间</td><td>判断 Broker 是否成为瓶颈</td></tr></tbody></table><p>如果磁盘即将打满，不能只靠手工删除日志目录救急。正确处理通常是组合动作：降低保留时间或大小、扩容 Broker、迁移热点分区、限制异常 Producer、优化压缩策略、清理无效 Topic，并确认副本重分配不会在短时间内制造更大的磁盘峰值。</p><h2 id="十二、面试表达与排查口径" tabindex="-1">十二、面试表达与排查口径 <a class="header-anchor" href="#十二、面试表达与排查口径" aria-label="Permalink to &quot;十二、面试表达与排查口径&quot;">​</a></h2><p>被问到“Kafka 为什么快”时，可以按照这条线回答：Kafka 以 Partition 为单位追加写日志，避免大量随机写；Producer 和 Broker 都尽量批量处理，减少请求和系统调用；Broker 利用 Page Cache 承接写入和读取热点；Consumer 读取时通过 Offset 稀疏索引定位，再顺序扫描；文件到网络发送路径可以利用零拷贝减少 CPU 拷贝和上下文切换。最后再补充一句，Kafka 的高性能不是牺牲持久化换来的，而是建立在日志模型、批量处理、操作系统缓存和副本机制的组合上。</p><p>被问到“消息消费后为什么还在 Kafka 里”时，要强调 Kafka 的删除不跟单个消费者绑定。消息生命周期由 Topic 保留策略、Segment 状态和日志清理策略决定；Consumer 只是提交自己的消费 Offset，不会删除 Broker 上的消息。这也是 Kafka 能支持多消费组、回放、重建下游和故障恢复的原因。</p><p>被问到“Log Compaction 和普通删除有什么区别”时，可以用事件流和状态流区分：普通删除保留最近一段时间的完整事件，适合日志和流水；Compaction 保留每个 Key 的最新状态，适合配置、维表和状态同步。Compaction 是异步清理，不保证消费者只读到最新值。</p><p>本章需要掌握的主线是：Broker 通过 Partition 日志组织数据，通过 Segment 控制文件粒度，通过稀疏索引定位 Offset，通过顺序写、Page Cache 和零拷贝提升读写吞吐，通过副本与确认语义定义写入成功，通过保留策略和 Compaction 管理消息生命周期，通过 Controller/KRaft 维护集群元数据。把这些点连起来，才能解释 Kafka 为什么既是消息队列，又更像一个可回放的分布式日志系统。</p>',69)])])}const s=e(d,[["render",i]]);export{c as __pageData,s as default};
