# 2026-09-08

## 抓取 hxsay.com「浣熊指南」央国企知识库

- 目标站点：https://www.hxsay.com/blog_page?bucket=hxsay&prefix=blogs/浣熊指南/ （Vue SPA，HTML 无内容）
- 逆向出的接口（需带浏览器 UA，否则返回 403「访问被拒绝」；另需 `X-Station-Device-Id` 头）：
  - `GET /api/blog-page/request-blog-nav-items?bucket=&prefix=` → 目录树
  - `GET /api/blog-page/request-blog-content?bucket=&prefix=&fileName=` → 全文（需登录 token，`Authorization` + `Sa-Token` 头，未登录返回 401）
  - `GET /api/blog-page/request-blog-preview` → 免登录，但**内容被截断到约 2000 字**
- 抓取脚本：`fetch.py`（支持 `HXSAY_TOKEN` 环境变量，`python fetch.py full` 抓全文；含 429 退避重试）
- 结果：`docs/` 下按 9 个分类归档 60 篇 md（生存指南为占位空目录），另生成 `docs/00-目录.md` 索引与 `_抓取报告.csv`
- **已完成全文版**：用户提供登录 token 后重跑 `HXSAY_TOKEN=xxx python fetch.py full`，60 篇全部为全文，共约 21.5 万字；配图 2 张已下载到 `docs/_images` 并把链接改为相对路径
- 注意：图片域名为 `https://hxsay.com:19000/...`，curl 需加 `-k`
- 复用：改 `ROOT_PREFIX` 可抓该站其他知识库目录；token 过期后重新取 `localStorage.token` 的 `value` 字段即可

## 抓取「浣熊专业课」（新建独立目录 `浣熊专业课/`）

- 命令：`HXSAY_TOKEN=xxx python fetch.py full "blogs/浣熊专业课/" "浣熊专业课"`（fetch.py 已支持 `[full|preview] [prefix] [outdir]` 三个位置参数）
- 结果：**64 篇全文 / 88.8 万字**，8 个分类（数据结构含「6、树和二叉树」「7、图」两级子目录，已按原结构保留）；`信息新技术` 为空占位目录
- 图片处理：`fix_images.py <目录>` 把正文里的 `<img type="raw" value="...">` 内嵌 HTML 块（HTML 转义过）还原成标准 `![](...)`，并并发下载图片到 `_images/`。专业课 580 张全部成功（约 195MB），指南目录 55 张也已处理
- 踩坑：图片域名 `https://hxsay.com:19000/hxsay-image/...`，需禁用证书校验；URL 里含 `%2F` 编码斜杠，取 basename 时要按字面处理
- 注：postprocess（补 H1 + 本地化图片 + 生成 `00-目录.md` 索引）已内置在 fetch.py，重跑时已存在的文件会 SKIP 不重复下载

## 正文清洗（收尾）

- `fix_images.py <目录>`：同时处理**转义**（`<img type="raw" value="...">`）和**未转义**（原生 `<div><img></div>`、零散 `<img>`）两种图片块，已下载的图片会跳过
- `fix_tables.py <目录>`：多单元格真表格 → Markdown 表格；单单元格块（题库，含多个 `<p>`）→ 摊平成段落；顺带清 `<br>` 和 `&gt;` 等实体
- 最终校验：两个目录 HTML 残留 0 命中；700 处本地图片引用 0 缺失；64 篇 / 86.98 万字 + 60 篇 / 16.3 万字
- 踩坑：fetch.py 早期版 localize 写死了 `_images/` 没加 `../`，导致子目录文件图片路径错，已改成按深度计算
- 已沉淀为技能 `~/.workbuddy/skills/hxsay-downloader/`（含 SKILL.md + scripts/fetch.py、fix_images.py、fix_tables.py）

## 抓取专项练习题库（exam-practice）

- 门户接口 `GET /api/edu/exam-portal/app/special-portal` → catalogSections：央国企基础（行测/时政/企业文化/公基/写作题）+ 专业课（计算机类等）；每个 card 的 `papers[].description` 里含「题库ID=xxx」
- 题目接口 `GET /api/recruitQuestion/listByQuestionBankId?questionBankId=&pageNum=&pageSize=` → `{records,total,pages}`；`listByPaperId?paperId=` 也有
- 题型映射：`1=单选 2=多选 3=判断 5=翻译 6=材料单选 7=写作`；**写作/翻译不是选择题**（answer 是整篇范文）
- 图片字段 `imageUrl` 是 JSON 数组字符串，值为 `/api/fileContainer/object-preview?bucket=hxsay-image&object=...`，拼 BASE 后带 token 可下载
- **坑：站点自己的 `dedupeKey` 不可靠**（图形推理 226 题只有 2 个值），必须用「题干+答案+选项+图片」自建指纹，否则图形推理题会被误合并
- 结果：`题库/questions.json` **7847 题**（行测1584/企业文化3254/计算机类2929/公基80；单选6781+材料77+多选385+判断604），8.1MB + 254 张图 9.1MB；脚本 `crawl_questions.py`
- 用户意向：想把这部分做成**本地刷题 Web 应用（只做选择题）**；已确认不需要数据库（JSON+localStorage 足够），待确认功能范围后实现

## 构建本地刷题 Web 应用（已完成）

- 产物目录：`刷题/` → `index.html`（单文件应用）+ `questions.js`（`window.QUESTIONS` 内联，file:// 双击直开免服务器）+ `images/`（254 张）
- 功能：练习（即时判题+自动跳题）、背题（直接看答案）、组卷（限时/交卷判分/查看解析）、错题本、收藏、统计（分类掌握度+题库进度）、设置（导入导出进度/清空）
- 题型覆盖：单选6781 / 多选385 / 判断604 / 材料单选77；判题用 `normAns()` 归一化（大写+去非A-E+排序）以正确识别多选乱序
- **关键逻辑修正**（已做 jsdom 冒烟测试 19 项全过）：
  - 组卷（exam）模式点击选项只记录 `SES.ans`、不渲染 right/wrong 高亮（考完才判分），且允许改答案
  - 背题模式多选题答案高亮（原只高亮首个字母）
  - 解析（review）模式不重复计入答题记录（judge 仅在 `mode==='practice'` 时 record）
- 验证：jsdom 加载 index.html 跑通 练习→答题→组卷→交卷→解析→背题 全流程；Node 校验题目字段零缺失、答案均在选项内
- 生成器：`scripts/make_quiz.py`（在 hxsay-downloader 技能里），读 `题库/questions.json` 一键产出 `刷题/`；模板 `scripts/quiz_template.html`
- 已把「题目接口+刷题App」补进技能 `~/.workbuddy/skills/hxsay-downloader/SKILL.md`

## 项目整体迁移到 D 盘

- 用户要求把整个 `央国企指南手册` 工程迁移到 `D:\学习资源\秋招\央国企指南手册\`（作为子文件夹整包放入，非平铺）。
- 操作：robocopy /MOVE 跨盘拷贝 1344 文件 / 253.84MB 到 `D:\学习资源\秋招目前\央国企指南手册`（误建了秋招目前，后同盘 mv 到 `D:\学习资源\秋招\` 并删掉空的秋招目前）。
- 验证：项目内无写死绝对路径（全相对路径，刷题 App 用 `questions.js`+`images/` 相对引用），迁移后应用照常运行；目标文件总数 1344、questions.js 4.7MB 完整。
- 桌面快捷方式 `央国企刷题.url` 已按新路径重建（file:///D:/.../秋招/央国企指南手册/刷题/index.html），旧 C 盘指向已失效。
- 注意：C 盘 `C:\Users\18584\WorkBuddy\央国企指南手册` 现为空壳（被 WorkBuddy IDE 占用句柄删不掉，无数据），关闭 IDE/重开项目后可手动删除。
- 教训：生成临时脚本时 Write 路径写成了旧 C 盘目录，导致误建空 `刷题` 子目录并残留；应在目标新路径下生成脚本。

## 刷题 App 增加「资料库」入口（四川中烟工业 D 类）

- 需求：在 `刷题/index.html` 顶部导航增加入口，能在刷题页里打开 `D:\学习资源\秋招\2025年四川中烟工业D类`（含 PDF/讲义/填空等，非选择题）并优化 PDF 阅读体验。
- 实现：导航加 `📚 资料库` 标签 → `vLib()` 用 iframe 嵌入 `../../2025年四川中烟工业D类/index.html`（两目录同挂 `秋招` 下，相对路径正确）。资料库顶部有蓝色「↗ 在新窗口打开」按钮做兜底。
- 优化四川中烟 viewer（`2025年四川中烟工业D类/index.html`，由 `make_viewer.py` 生成）：上一篇/下一篇导航+键盘←/→、专注模式（隐藏侧栏+全屏铺满，iframe 内也可靠）、记住上次打开文件重开即恢复、双击文件直接新窗口打开、路径面包屑、搜索回车开首个结果、内嵌预览区空白时提示点「📑 新窗口打开」用原生阅读器。
- 坑：第一次只加了导航按钮、漏写 `vLib()` 和路由映射，导致点击走兜底显示回刷题首页（用户反馈“资料库不是中烟内容”），已补 `lib:vLib` 到 dispatch 并定义 `vLib()`。
- 注意：Chrome/Edge 对 `file://` 跨目录 iframe 可能拦截，内嵌区或空白；此时点「新窗口打开」在独立标签打开完整优化版即可。如需永远内嵌可见，可改为在刷题页原生渲染文件树+点击新窗口开 PDF。

## 资料浏览器二次优化（PDF 留白 / 侧栏隐藏 / 翻篇可见文件名）

- 用户反馈：内嵌 PDF 两侧灰白太多、侧栏要能隐藏、隐藏后切下一个要看得到下一个文件名。
- 改动 `make_viewer.py`（重跑再生 index.html，86 文件 2 目录不变）：
  1. PDF iframe 加 `#view=FitH&toolbar=0&navpanes=0&zoom=page-width`，强制横向铺满、去掉自带导航，消除两侧灰白。
  2. 顶部加 ☰ 按钮 + 左缘窄把手，可隐藏/展开侧栏（body.noside）；嵌入式环境（iframe 内）**默认 noside**。
  3. 工具栏新增 `curinfo`（蓝底高亮当前文件名）与上一篇/下一篇按钮**直接显示对方文件名**（truncate 14 字），隐藏侧栏也能看清在哪一页、下一页是谁。
  4. 顶栏可隐藏（body.hidetop，按钮「—」或 H 键）；S 键切侧栏、H 键切顶栏、←/→ 翻篇保留。
- 同步把刷题 App 的 `vLib()` iframe 高度从 `calc(100vh-220px)` 提到 `-175px`、卡片下边距 12→8px，内嵌区更满。
- 校验：PH 占位符 0、JSON 解析 OK、无未定义 readBtn 引用、toggle 全部接通。

## 用 docs/ 搭建 VitePress 文档站（已完成）

- 目标：把 `docs/` 60 篇 md 做成可本地预览+一键构建的静态站点（类 llm123.yikuyiku.com 文档站）。
- 结构：VitePress 工程直接建在 `docs/` 内 —— `docs/package.json`(type:module, 脚本 docs:dev/build/preview)、`docs/.vitepress/config.js`、`docs/index.md`(layout:home)、`docs/.gitignore`(忽略 node_modules/.vitepress/dist/cache)。`docs/` 既是内容源也是站点根，构建产物 `docs/.vitepress/dist`。
- 侧栏：用 `vitepress-sidebar` 的 `generateSidebar()`（注意导出名是 `generateSidebar` 不是 `createSidebar`）按目录自动生成；`search:{provider:'local'}` 内置全文搜索。
- 依赖：node 22.22.2 管理的 npm 在 `docs/` 本地 `npm install`（145 包），vitepress ^1.6.3 + vitepress-sidebar ^1.28.0。
- 两个必踩坑已修复：
  1. md 里图片相对 `../_images/quesion%2Fimgs%2F<hash>.png`，但文件名字面含 `%2F`，Rollup 会把 `%2F` 解码成 `/` 当子目录导致构建失败。用 Python 脚本把 96 个图片文件名 `%2F`→`_` 并把 29 个 md 里的引用同步替换（`quesion%2Fimgs%2F`→`quesion_imgs_`），问题解决（dist/assets 已含 quesion_imgs_*.png）。
  2. 原始博客文章间互链（`./上岸攻略总览`、`./考试类` 等）在子集归档里目标页不存在，VitePress 死链检查会阻断构建 → config 加 `ignoreDeadLinks:true`。
- 构建结果：63 个 HTML 页 + 232 个资源（图片）；`npm run docs:build` 通过；`npm run docs:preview --host` 已在后台跑（端口 4173，绑定 0.0.0.0 供手机同局域网访问）。
- 移动端：VitePress 天生响应式，手机浏览器可直接看；但要「手机访问」需 (a) 同 Wi-Fi 下开 `docs:dev --host` 或 `docs:preview --host` 后用电脑局域网 IP:端口打开，或 (b) 部署到 GitHub Pages/GitCode Pages/Netlify 等公开站点。不一定非要传 GitHub/GitCode——本地+局域网即可；只有想公网/手机随时访问才需托管。
- 若以后加新 md，重跑 `npm run docs:build` 即可；`generateSidebar` 会自动收录。

## 抓取 7 个面试/行测/AI/算法/Java 板块（已完成，全量干净）

- 用户最初以为「结构化/半结构化/无领导小组」同挂一个链接；后纠正：**三者是三个独立链接**（分别 `blogs/结构化面试/`、`blogs/半结构化面试/`、`blogs/无领导小组面试/`）。
- 最终 7 个板块全部落到 `D:\学习资源\秋招\央国企指南手册\`：
  - 无领导小组面试 7 篇 / 7 图；结构化面试 6 篇 / 7 图；半结构化面试 8 篇 / 5 图
  - 浣熊行测 52 篇 / 179 图（1 篇 `2.空间推理.md` 是空占位，跳过）；浣熊AI应用开发 36 篇 / 99 图
  - 算法笔试LeetCode 13 篇 / 0 图；Java面试问题汇总 1 篇 / 0 图
  - 每个板块均带 `00-目录.md` 索引与 `_images/`。
- 踩坑与修复（已沉淀进技能 `hxsay-downloader`）：
  1. **图片 presigned 签名过期**：URL 带 `?X-Amz-Expires=604800`（7 天），抓取时早过 → 403。修复：`fix_images.py` 的 `download()` 原 URL 失败后自动去查询串公开读重试（浣熊AI 51 张、结构化 7 张靠此挽回）。
  2. **raw board 块「截断残留」**：`<img type="raw" value="...">` 内层 `<div style="...">` 使老正则 `(.*?)"` 停在内层 `">`，残留 `<img src>...</div>"`。修复：`fix_images.py` 的 `BLOCK` 改为锚定 `(?:</div>|&lt;/div&gt;)\s*">` 吃全整块 + 深度 `html.unescape`（双重转义也覆盖）。
  3. **`00-` 误删文章**：`fetch.py`/`fix_images.py` 原 `startswith("00-")` 把 `00-RAG基础学习路线.md` 等正文章排除 → 改成只排除 `00-目录`。
  4. **highlight 块 / 双重转义残留**：`fix_images`/`fix_tables` 不处理 `<div class="hxsay-highlight-block">` 与漏网的双重转义块。新增 `clean_pass2.py`（技能脚本）作二次清洗：highlight 块→Markdown 引用块、raw/转义块→本地 `![](_images/..)`、清 `board-*.png` 独立行。
- **二次清洗实操**：本会话对 `结构化面试`(6) + `半结构化面试`(7) 跑 `clean_pass2.py`，全 7 板块 HTML 残留复查归 0、board 独立行归 0、所有图片引用 0 缺失。
- 技能目录已更新：`scripts/clean_pass2.py` 新增；`fetch.py`/`fix_images.py` 上述 3 处修复；`SKILL.md` 补「步骤5 二次清洗」「presigned 过期兜底」「raw 块截断坑」说明。
- 注意：临时脚本（`_clean_pass2.py`/`_recover_imgs.py`/`_finalize_index.py`）用完即删，正式逻辑已入技能，勿在 BASE 留散脚本。
