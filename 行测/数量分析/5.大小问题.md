# 大小问题

比较大小是一种常见的问题，但是行测考试中一般不可能考察像什么3和5的大小这种傻子都能看出来的问题，一般还是需要些技巧的，所以大家遇到比较大小的题目首先就需要思考的是考察的是什么样的技巧，以及该怎么样子去解决。

![](../_images/quesion_imgs_8e82fbe00e6641c69ab4ef5b5ff2e457.png)

一般而言，行测当中比较大小的解决办法有六种，作差法、作商法、中间值法、倒数法、不等式法、差值比较法等，其中主要需要把握的是中间值法、倒数法和不等式法3中即可，考得比较多。

## 知识要点

1.  **作差法**：对任意两数 、，如果 则 ；如果 则 ；如果 则 。

1.  **作商法**：

    -   当 、 为任意两正数时，如果 则 ；如果 则 ；如果 则 。
    
    -   当 、 为任意两负数时，如果 则 ；如果 则 ；如果 则 。

1.  **中间值法：** 对任意两数 、，当很难直接用作差法或者作比法比较大小时，我们通常选取中间值 ，如果 而 ，则我们说 。

1.  **倒数法：** 通过比较两个数的倒数来判断大小,当 a 和 b同号时，若 ，则 ；若 ，则 。

1.  **不等式法：** 利用不等式的性质来比较大小，原理如下：

    -   若 a≥b，则 ；
    -   若 a≥b 且 c≥d，则 ；
    -   若 a>b 且 c>0，则 ；
    -   若 a>b 且 cb>0 且 c>d>0，则 ；
    -   若 a>b>0，则 ；
    -   若 a>b>0，则 ；
    -   当 且 n>0 且 n为偶数时，
    -   若 a>0且 b>0，则 a≥b>0；
    -   若 a0 且 n 为奇数时，则 a≥b。

1.  **差值比较法**：通过比较几个分数与某个固定值（如1）的差值来判断大小。通常情况下，比较几个分数的大小时，如果其值与“1”或某一个整数比较接近，则可以通过比较这几个分数与“1”的差值来判断它们的大小。

## 例题解析

例1：分数 、、、、 中最小的一个是：

解析：选用中间值法。取中间值 和原式的各个分数进行比较，我们可以发现,正确答案为

例2： 比较大小： ,

解析：选用作比法。

-   所以，

例3：π，3.14， ，10/3 四个数的大小顺序是：

**解析：** 显然可知 10/3 > π > 3.14，所以此题的关键是比较 和 10/3 的大小以及 和 π 的大小。

-   首先观察 和 10/3 是两个正数，可以运用作比法也可以运用作差法，但显然作差法不宜判断，故选用作比法：
-   对于 和 π 的大小比较，我们选取中间值 3.15，显然 3.15 > π 而 ，所以 3.15  > π > 3.14。
