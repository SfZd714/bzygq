# 部署说明（手机可在线阅读）

本目录是一个 [VitePress](https://vitepress.dev/) 静态文档站，已把全部 Markdown 教程（含本地图片 `_images/`）合并为统一站点，自动生成侧栏导航、本地搜索，响应式适配手机。

**当前主方案：GitHub Pages**（免费、无需实名）+ **Cloudflare Pages**（国内加速，互为备份）。

---

## 一、在本机构建验证（可选）

```bash
npm install
npm run dev          # 本地预览，默认 http://localhost:5173
# 或手机同 Wi-Fi：npm run dev -- --host  然后手机访问 http://<电脑局域网IP>:5173
npm run build        # 产出 .vitepress/dist 静态文件
# 预览已构建产物：npm run preview  （手机访问加 --host）
```

> 注意：在 WorkBuddy 终端里构建需先 `export CODEBUDDY_SAFE_DELETE_ENABLED=0`，否则 Vite 清理 dist 时会被 safe-delete 拦截导致构建失败（无该 shim 的环境忽略即可）。

---

## 二、部署到 GitHub Pages（免费，无需实名/付费）

费用与认证：**公开仓库的 GitHub Pages 完全免费，不需要实名认证**。只有私有仓库才需付费套餐。

### 1. 建空仓库（仅首次）

打开 https://github.com/new ，仓库名 `bzygq`，**不要勾** README / .gitignore / License（保持完全空）→ Create repository。

### 2. 关联远程并推送（仅首次）

```bash
git remote add github git@github.com:SfZd714/bzygq.git
git push -u github master        # 源码备份（不含图片，图片只在 gh-pages 分支）
```

> 前提：本机 SSH 公钥已加到 GitHub。验证：`ssh -T git@github.com` 应返回 `Hi SfZd714!`。

### 3. 一键部署（构建并把 dist 推到 gh-pages 分支）

```bash
bash deploy-github.sh
```

- 脚本自动按仓库名推导 `base`：`bzygq` → `/bzygq/`，用户页仓库 `SfZd714.github.io` → `/`。
- 也可手动指定：`GITHUB_REMOTE=git@github.com:SfZd714/bzygq.git bash deploy-github.sh`。
- 脚本会自动写入 `.nojekyll`，避免 GitHub 的 Jekyll 处理吞掉下划线目录。

### 4. 开启 Pages（网页操作）

GitHub 仓库 → **Settings** → **Pages** → Build and deployment：

- Source 选 **Deploy from a branch**
- Branch 选 **`gh-pages`**、目录 **`/ (root)`** → **Save**

等 1~3 分钟，访问 **https://SfZd714.github.io/bzygq/**

---

## 三、Cloudflare Pages（国内加速，推荐同时开）

GitHub Pages 在国内可能偏慢或不稳。Cloudflare Pages 免费、无需实名（邮箱注册即可），可从 GitHub 仓库直接导入，国内访问明显更稳。

### 步骤

1. 注册 https://dash.cloudflare.com/sign-up （邮箱即可，无实名）
2. **Workers & Pages** → **Create** → **Pages** → **Connect to Git**
3. 授权 GitHub，选中 `bzygq` 仓库
4. 构建配置（VitePress 预设或自定义）：
   - Framework preset: `VitePress`（没有就选 `None`）
   - Build command: `npm run build`
   - Build output directory: 填 **`.vitepress/dist`**
   - 环境变量加一条：`PAGES_BASE` = `/` （Cloudflare 给的是根域名，无需子路径）
5. Save and Deploy，等 1~2 分钟，得到 `https://bzygq.pages.dev/`

> `.vitepress/dist` 这个输出目录是关键——默认填 `dist` 会构建失败，因为 VitePress 1.x 产物在 `.vitepress/dist`。
>
> 注意：Cloudflare 从源码构建需要仓库里有 `package.json`（master 分支已有），且源码分支不含图片（`_images/` 已 gitignore）会导致**构建时缺图**。若走 Cloudflare 构建路线，需把 `_images/` 也纳入 git；或者改用「直接上传 dist」的方式（Wrangler / 拖拽上传）。

### 自定义域名（可选）

Cloudflare Pages → Custom domains → 绑定自己的域名即可。

---

## 四、Gitee Pages（暂未开通，需实名认证）

- Gitee Pages 属于付费能力范畴，**账号必须先完成实名认证**，否则「服务」菜单里连 Gitee Pages 图标都不会出现，直链 `/pages` 直接 404。
- 已推送的分支：`master`（源码）、`gh-pages`（构建产物）。
- 若日后完成实名认证：Gitee 仓库 → 顶部 **服务** → Gitee Pages → 部署分支 `gh-pages`、目录 `/` → 启动 → 访问 `https://wzx825.gitee.io/bzygq/`
- 重新推送用：`bash deploy-gitee.sh`

### Gitee 推送鉴权坑（已踩过）

Gitee **已停用账号密码做 HTTPS 鉴权**，用密码推送会返回 `401 Unauthorized`；非交互环境下还会**静默挂死到超时**（表现为无输出、退出码 124，极具迷惑性）。**必须用 SSH 或私人令牌**。

---

## 五、手机阅读

- 部署后直接用手机浏览器打开站点地址即可。
- 站点响应式：手机上侧栏收成汉堡菜单，单列排版，支持本地搜索、大纲跳转。
- 未部署时，电脑开 `npm run dev -- --host`，手机同 Wi-Fi 访问局域网地址也能看（零托管）。

---

## 六、更新内容后重新发布

改完 Markdown / 图片后：

```bash
bash deploy-github.sh     # 重新构建并推送 gh-pages（GitHub Pages 会自动更新）
```

若只改源码想保留到 master：`git add -A && git commit -m "..." && git push github master`。

---

## 七、常见问题

| 现象 | 原因与解决 |
|---|---|
| 构建报 `safe-delete` / EPERM | 未关闭 WorkBuddy 的 safe-delete 拦截。构建前 `export CODEBUDDY_SAFE_DELETE_ENABLED=0`（两个部署脚本已内置） |
| 页面 404 / 样式全丢 | `base` 与仓库名不匹配。项目页 base 必须是 `/<仓库名>/`。重跑部署脚本会自动修正 |
| CSS/JS 404 | 忘了 `.nojekyll`（GitHub）或输出目录填错（Cloudflare 应为 `.vitepress/dist`） |
| 图片不显示 | `gh-pages` 分支需包含图片。脚本从 `.vitepress/dist` 整体拷贝，已含全部图片；源码分支不存图片（已 gitignore） |
| 搜索不可用 | 本地搜索索引在构建时生成，确认用的是 `npm run build` 产物而非手动拷贝的 md |
| Gitee 推送无输出、超时退出 124 | HTTPS 密码鉴权被停用 + 非交互等待输入。改用 SSH：`git remote set-url gitee git@gitee.com:wzx825/bzygq.git` |
